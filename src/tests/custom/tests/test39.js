// jstyper start
var fooObjs = [{foo:1}, {foo:2, bar:39}];
// NB JSTyper assumes the array type to be the first element it sees, so it
// wouldn't support the elements in the other order (proven in 39b)

// jstyper end